<head>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">

</head>


<body>


<div class="wrapper">
  <!-- Top Navbar -->
  <nav class="navbar navbar-expand navbar-dark bg-dark">

    <!-- Brand logo -->
    <a class="navbar-brand" href="#">
      <%= image_tag("logos/logo-64x64_inverted.png", alt: "Digital Synapse Logo", width: '24', height: '24') %> Alexandria
    </a>

  </nav>
  <!-- End top navbar -->



  <!-- Page content -->
  <nav id="content">
    <!-- Main page section -->
    <div class="container">
      <div class="row">
        <div class="col-sm-12">
          <% if @books.size <= 0 %>
              <h1>No Books</h1>
          <% else %>
              <h1><%= pluralize(@books.size, 'Book') %></h1>

              <!-- <table class="table table-striped"> -->
              <table class="table table-striped table-bordered table-hover" id="books-dataTable">
                <thead>
                <tr>
                  <th>#</th>
                  <th>Name</th>
                  <th>Description</th>
                  <th>Book Type</th>
                  <th>Paid</th>
                </tr>
                </thead>
                <tbody>

                <% @books.each do |book| %>
                    <% if book.id.even? %>
                        <tr class="odd GradeX">
                    <% else %>
                        <tr class="even GradeX">
                    <% end %>
                    <td><%= book.id %></th>
                    <td><%= book.name[0..40] %></td>
                    <td><%= book.description[0..30] %></td>

                    <% if book.chapter? %>
                        <!-- <td style="text-align: center;"><i class="fa fa-book" aria-hidden="true"></i></td> -->
                        <td style="text-align: center;"><%= image_tag("icons/flat/book_chapter_color.svg", alt: "Chapter Book", width: '24', height: '24') %></td>
                    <% else %>
                        <td style="text-align: center;"><%= image_tag("icons/flat/book_nochapter_color.svg", alt: "Non-Chapter Book", width: '24', height: '24') %></td>
                    <% end %>



                    <% if book.paid? %>
                        <td style="text-align: center;"><%= image_tag("icons/flat/badge_rewarded.svg", alt: "Non-Chapter Book", width: '24', height: '24') %></td>
                    <% else %>
                        <td style="text-align: center; filter: grayscale(100%);"><%= image_tag("icons/flat/badge_rewarded.svg", alt: "Non-Chapter Book", width: '24', height: '24') %></td>
                        <!-- <td><%= book.paid %></td> -->
                    <% end %>
                    </tr>
                <% end %>
                </tbody>
              </table>
          <% end %>
        </div>
      </div>

    </div>
    <!-- End Page Content -->



  </nav>










</body>

